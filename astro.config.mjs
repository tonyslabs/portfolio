import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // TODO: set your real production URL for proper canonical links and RSS/sitemaps
  site: 'https://example.com',
});
